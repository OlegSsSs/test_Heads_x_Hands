public class Player extends BaseHero {

    public Player(String name, int attack, int defence, int health, int minDamage, int maxDamage, int maxHealthAttempts, double healPercent) {
        super(name, attack, defence, health, minDamage, maxDamage, maxHealthAttempts, healPercent);
    }
}


